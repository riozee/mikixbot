<!DOCTYPE html>
<head>
    <%- include('../partials/head'); %>
    <style>
        .center {
            position: relative;
            width: 100%;
            height: 92.5vh;
        }
        .center div {
            position: absolute;
            display: block;
            text-align: center;
            color: rgb(var(--secondary-color));
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            -o-transform: translate(-50%, -50%);
            -moz-transform: translate(-50%, -50%);
            -webkit-transform: translate(-50%, -50%);
            -ms-transform: translate(-50%, -50%);
        }
        .center div span h1 {
            padding: 0.2rem 0.5rem 0.2rem 0.5rem;
            margin-bottom: 0.5rem;
            background-color: rgba(var(--primary-color), 0.75);
            border-radius: 10px;
            backdrop-filter: blur(5px);
            -webkit-backdrop-filter: blur(5px);
            font-family: VT323, monospace;
            font-size: 15vmin;
        }
        .center div h2 {
            font-size: 7.5vmin;
        }
    </style>
    <title>Beranda | Miki Bot</title>
</head>
<body>
    <%- include('../partials/body') %>
    
    <div class="center">
        <div>
            <span><h1></h1></span>
            <h2>Welcome</h2>    
        </div>
    </div>

    <script>
        setInterval(function () {
            $('.center div span h1').text(new Date().toLocaleTimeString().replaceAll('.', ':') + '.' + new Date().getMilliseconds().toString().padStart(3, '0'));
        }, 1);
    </script>
</body>